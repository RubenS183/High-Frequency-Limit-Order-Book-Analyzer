"""Benchmark: ingestion message parsing and normalisation throughput."""
import pytest


@pytest.mark.benchmark
def test_bench_ingestion_parsing(benchmark) -> None:  # type: ignore[no-untyped-def]
    """Placeholder benchmark for ingestion normaliser."""
    assert True
