"""Benchmark: feature computation latency per snapshot."""
import pytest


@pytest.mark.benchmark
def test_bench_feature_pipeline(benchmark) -> None:  # type: ignore[no-untyped-def]
    """Placeholder benchmark for feature pipeline throughput."""
    assert True
